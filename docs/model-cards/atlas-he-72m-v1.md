# Atlas-H&E 72M distillation candidate

Status: `not-evaluable`. Training has not started because independent baseline packs and lawful
teacher derivative rights are not frozen. This manifest records the gate; it is not a trained
model. A future local probe must remain resumable, stay within the P2000 envelope, improve held-out
validation, and satisfy the frozen retrieval, dense-cell, brightfield, OOD, size, and latency gates.

